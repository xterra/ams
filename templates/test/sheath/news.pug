doctype html
html
    head
      title "NEWS page"
    body
      h1="NEWS"
      p #{sessionLogin}
      hr
      if sessionLogin=='admin'
        a(href="/create_news/")
          button(type="button") Create news
      each pieceOfNews in news
        h2 #{pieceOfNews.title}
        p  #{pieceOfNews.text}
        hr
      else 
        li There are no news.
    
